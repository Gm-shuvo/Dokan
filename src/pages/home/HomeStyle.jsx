import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  root:{
    marginTop: "100px",
    display: "flex",
    flexDirection: "column",
    placeItems: "center",
  },

}));